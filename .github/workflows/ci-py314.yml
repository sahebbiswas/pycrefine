name: CI Py3.14

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  call-workflow:
    uses: ./.github/workflows/reusable-test.yml
    with:
      python-version: '3.14'